# API routes should not be redirected
/api/*  /api/:splat  200
/functions/*  /functions/:splat  200

# SPA fallback for client-side routing (exclude API routes)
!/api/*
!/functions/*
/*    /index.html   200